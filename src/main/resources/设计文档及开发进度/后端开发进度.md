# RAG 知识助手 Demo 后端开发进度跟踪文档

本文档以后端对外暴露的 REST API 接口为最小追踪单位，按照开发顺序依次排列。优先开发基础配置与核心对话流程所需接口，再扩展知识库管理相关的增删改查能力。每完成一个接口的开发与自测后，请及时更新状态与完成时间。

## 第一阶段：项目基础与核心对话流程

本阶段目标是快速打通“用户提问 → 向量检索 → LLM 生成 → 流式返回”的最小闭环。

| 序号 | 接口名称 | 请求方式 | 路径 | 优先级 | 状态 | 开始时间 | 完成时间 | 说明 |
|:----:|:---------|:--------:|:-----|:------:|:----:|:--------:|:--------:|:-----|
| 1 | 项目初始化与依赖配置 | — | — | P0 | 待开始 | | | 完成 Spring Boot 项目搭建，引入 Spring AI、Ollama、Chroma、Tika 等核心依赖 |
| 2 | 应用配置属性绑定 | — | — | P0 | 待开始 | | | 实现类型安全的配置类，管理检索参数、提示词模板、文件路径等 |
| 3 | LLM 客户端封装 | — | — | P0 | 待开始 | | | 封装 ChatClient Bean，配置 Ollama 连接，支持流式生成调用 |
| 4 | 向量存储配置 | — | — | P0 | 待开始 | | | 配置 ChromaDB 连接，初始化 VectorStore Bean，指定集合名称 |
| 5 | 会话记忆存储配置 | — | — | P1 | 待开始 | | | 配置 H2 数据源与 JdbcChatMemoryRepository，启用会话持久化 |
| 6 | RAG 核心编排服务 | — | — | P0 | 待开始 | | | 实现 RAGService，绑定 QuestionAnswerAdvisor，串联检索与生成 |
| 7 | 流式对话接口 | POST | `/api/chat/stream` | P0 | 待开始 | | | 接收用户消息与会话 ID，通过 SSE 流式返回 LLM 生成的答案片段 |

## 第二阶段：会话管理接口

本阶段目标是为前端侧边栏提供会话历史管理能力。

| 序号 | 接口名称 | 请求方式 | 路径 | 优先级 | 状态 | 开始时间 | 完成时间 | 说明 |
|:----:|:---------|:--------:|:-----|:------:|:----:|:--------:|:--------:|:-----|
| 8 | 获取会话列表 | GET | `/api/conversations` | P1 | 待开始 | | | 返回当前所有会话的 ID、标题、更新时间等简要信息 |
| 9 | 获取会话历史消息 | GET | `/api/conversations/{id}/messages` | P1 | 待开始 | | | 根据会话 ID 返回该会话下的完整消息记录 |
| 10 | 删除会话 | DELETE | `/api/conversations/{id}` | P1 | 待开始 | | | 删除指定会话及其关联的所有历史消息 |

## 第三阶段：知识库文档上传与索引接口

本阶段目标是为前端提供文档上传入口，实现文件到向量索引的自动处理。

| 序号 | 接口名称 | 请求方式 | 路径 | 优先级 | 状态 | 开始时间 | 完成时间 | 说明 |
|:----:|:---------|:--------:|:-----|:------:|:----:|:--------:|:--------:|:-----|
| 11 | 文档解析服务 | — | — | P0 | 待开始 | | | 集成 Apache Tika，实现从文件输入流提取纯文本的能力 |
| 12 | 索引构建服务 | — | — | P0 | 待开始 | | | 实现文本分块、调用 Embedding、写入 ChromaDB 的完整流程 |
| 13 | 文档上传并索引接口 | POST | `/api/knowledge/upload` | P0 | 待开始 | | | 接收上传文件，调用解析与索引服务，返回处理结果 |

## 第四阶段：知识库手动录入与在线管理接口

本阶段目标是为前端知识库管理中心提供完整的 CRUD 能力，支持手动录入文本及对已入库条目的查看、编辑、删除操作。

| 序号 | 接口名称 | 请求方式 | 路径 | 优先级 | 状态 | 开始时间 | 完成时间 | 说明 |
|:----:|:---------|:--------:|:-----|:------:|:----:|:--------:|:--------:|:-----|
| 14 | 手动录入知识条目接口 | POST | `/api/knowledge/manual` | P0 | 待开始 | | | 接收标题、内容、标签等字段，经分块与向量化后存入 ChromaDB |
| 15 | 分页查询知识条目列表接口 | GET | `/api/knowledge/items` | P0 | 待开始 | | | 支持分页参数及关键词检索，返回条目摘要信息 |
| 16 | 获取知识条目详情接口 | GET | `/api/knowledge/items/{id}` | P1 | 待开始 | | | 根据条目 ID 返回完整的文本内容与元数据 |
| 17 | 更新知识条目接口 | PUT | `/api/knowledge/items/{id}` | P0 | 待开始 | | | 修改条目内容后需重新向量化并更新索引 |
| 18 | 删除知识条目接口 | DELETE | `/api/knowledge/items/{id}` | P0 | 待开始 | | | 从 ChromaDB 中移除指定条目对应的所有向量数据 |
| 19 | 全局异常处理与统一响应封装 | — | — | P1 | 待开始 | | | 定义统一的 ApiResponse 结构，处理常见业务异常 |

## 第五阶段：联调与收尾

| 序号 | 任务项 | 优先级 | 状态 | 开始时间 | 完成时间 | 说明 |
|:----:|:-------|:------:|:----:|:--------:|:--------:|:-----|
| 20 | 后端全接口自测与联调 | P0 | 待开始 | | | 使用 Postman 或 Curl 逐一验证所有接口的正确性 |
| 21 | 与前端联合调试 | P0 | 待开始 | | | 配合前端页面完成端到端流程验证 |
| 22 | 编写 README 与接口文档 | P2 | 待开始 | | | 说明环境要求、启动步骤及接口调用示例 |

## 状态说明

- **待开始**：该接口或任务尚未启动任何开发工作。
- **进行中**：正在编写代码或调试过程中。
- **已完成**：代码已编写完毕，且通过基本的单元测试或自测。
- **已阻塞**：由于依赖组件未就绪或技术难点暂时无法推进。
